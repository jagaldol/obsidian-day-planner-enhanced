# CLAUDE.md

This project's agent guidance lives in `AGENTS.md` (the tool-agnostic source of truth, shared with Codex and other tools). It is imported below so Claude Code loads it automatically.

@AGENTS.md

Hierarchical, directory-level guidance is provided as `AGENTS.md` files throughout `src/` and `tests/`. When working in a subdirectory, read its local `AGENTS.md` for purpose, key files, and editing rules.

<!-- Add Claude Code-specific instructions below this line. Keep shared, tool-agnostic guidance in AGENTS.md. -->
